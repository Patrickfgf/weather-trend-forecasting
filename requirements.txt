# Core stack — pinned for reproducibility. Verified to have wheels on Python 3.14.
# A clean Restart-Run-All of the notebook / `python run_pipeline.py` needs only these.
# Optional accelerators & extras (lightgbm, prophet, shap, pandera, kaleido, folium,
# pycountry-convert) are in requirements-optional.txt and degrade gracefully if absent.
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.14.6
xgboost==3.2.0
matplotlib==3.11.0
seaborn==0.13.2
plotly==6.8.0
pyarrow==24.0.0
duckdb==1.5.3
# Notebook execution
jupyter==1.1.1
ipykernel==7.3.0
nbformat==5.10.4
nbconvert==7.17.1
# Testing
pytest==9.1.0
